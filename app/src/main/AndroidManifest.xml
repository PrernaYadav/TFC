<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.infosolution.dev.tfc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".business.Navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginMailActivity" />
        <activity android:name=".activities.SignupMailActivity" />
        <activity android:name=".business.LoginBusinessActivity" />
        <activity android:name=".business.SignupBusiActivity" />
        <activity android:name=".user.UserRegisterActivity" />
        <activity android:name=".user.UserRegNext" />
        <activity android:name=".activities.ProductPageActivity" />
        <!--
            <activity
            android:name=".navigation.Navigation"
            android:label="@string/title_activity_navigation"
            android:theme="@style/AppTheme"></activity>
        -->
        <!-- <activity android:name=".activities.CommentsActivity"></activity> -->
        <!-- <activity android:name=".activities.CommentActivity"></activity> -->
        <!--<activity android:name=".activities.BuyNowActivity"></activity>-->
    </application>

</manifest>